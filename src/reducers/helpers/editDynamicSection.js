
export const updateDynamicSections = (sections, payload) => {
    switch(payload.sectionAction) {
        case "Update": {
            
        }
        case "Add": {

        }
        case "Delete": {
            
        }
    }
    return sections
}